请帮我重新设计settings_ui.py的界面，将现有的7个tab简化为3个主tab，每个tab使用左侧边栏+右侧内容区的布局。

=== 整体结构 ===
使用QTabWidget，3个主tab：
- Tab 1: "🐾 桌宠设置"
- Tab 2: "💬 应用气泡"  
- Tab 3: "🤖 AI功能"

每个tab内部使用QSplitter，分为：
- 左侧：QListWidget（宽度200px，固定）
- 右侧：QStackedWidget（自适应宽度）

=== Tab 1: 桌宠设置 ===

左侧边栏选项（QListWidget items）：
1. "外观"
2. "行为"
3. "睡眠"
4. "提醒"

右侧内容页（QStackedWidget pages）：

【外观页】（暂时留空，未来可加主题、透明度等）

【行为页】
- 移动速度（QDoubleSpinBox，range 0.5-5.0，step 0.1，suffix " 倍速"）
- 自动行走开关（QCheckBox）
- 漫游半径（QSpinBox，range 0-2000，suffix " 像素"，tooltip: 0表示不限制）
- 边缘边距（QSpinBox，range 0-500，suffix " 像素"）
- AI轮询间隔（QSpinBox，range 200-10000，suffix " 毫秒"）

【睡眠页】
- 启用睡眠（QCheckBox）
- 闲置时长（QSpinBox，range 1-120，suffix " 分钟"）
- 肾上腺素时长（QSpinBox，range 1-60，suffix " 分钟"）
- 唤醒保护时长（QDoubleSpinBox，range 0-300，suffix " 秒"）

【提醒页】
- 喝水提醒开关（QCheckBox）
- 喝水间隔（QSpinBox，range 1-240，suffix " 分钟"）
- 运动提醒开关（QCheckBox）
- 运动间隔（QSpinBox，range 1-240，suffix " 分钟"）
- 活跃时段（两个时间选择：起始时:分 - 结束时:分）
- 提醒显示时长（QSpinBox，range 500-10000，suffix " 毫秒"）

=== Tab 2: 应用气泡 ===

左侧边栏选项：
1. "总开关 & 规则"
2. "应用映射"
3. "网站规则"
4. "过滤器"

右侧内容页：

【总开关 & 规则页】
- 启用应用气泡（QCheckBox，大号字体，醒目）
- 触发概率（QDoubleSpinBox，range 0.0-1.0，step 0.05）
- 显示时长（QSpinBox，range 500-20000，suffix " 毫秒"）
- 前台稳定阈值（QSpinBox，range 0-5000，suffix " 毫秒"）
- 最大待定数（QSpinBox，range 0-10）
- 各类别冷却时间（用QGroupBox包裹，标题"类别冷却时间"，内部用QFormLayout排列8个类别的SpinBox）

【应用映射页】
保留现有的list_apps + 编辑表单 + 自动抓取按钮的布局

【网站规则页】
保留现有的list_sites + 添加网站按钮的布局

【过滤器页】
保留现有的忽略exe列表 + 忽略关键词列表的布局

=== Tab 3: AI功能 ===

左侧边栏选项：
1. "连接配置"
2. "人格预设"
3. "高级选项"

右侧内容页：

【连接配置页】
- Provider选择（QComboBox）
- Base URL（QLineEdit）
- API Key（QLineEdit，setEchoMode QLineEdit.EchoMode.Password）
- 模型选择（QComboBox）
- 刷新模型列表按钮
- 测试连接按钮

【人格预设页】
- System Prompt（QTextEdit，高度150px）
- 预设列表（QListWidget）
- 预设管理按钮（添加、删除、重命名）
- 回复长度范围（两个QSpinBox：最小字数、最大字数）

【高级选项页】
- 自动巡视间隔（QSpinBox，range 0-120，suffix " 分钟"，0表示禁用）
- 记忆轮数（QSpinBox，range 0-20）
- 黑匣子日志数（QSpinBox，range 20-2000）
- 视觉支持检测（QCheckBox）

=== 样式要求 ===

整体背景色：#f5f5f5
卡片背景色：#ffffff

左侧边栏：
```css
QListWidget {
    background: white;
    border: none;
    border-right: 1px solid #e0e0e0;
    outline: none;
}
QListWidget::item {
    padding: 12px 16px;
    border-radius: 0px;
}
QListWidget::item:selected {
    background: #e3f2fd;
    color: #1976d2;
    font-weight: bold;
}
QListWidget::item:hover {
    background: #f5f5f5;
}
```

右侧内容区：
- 用QScrollArea包裹，setWidgetResizable(True)
- 内部用QVBoxLayout，每个设置项用QGroupBox或QFrame包裹
- GroupBox样式：
```css
QGroupBox {
    background: white;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
    padding: 16px;
    margin-top: 12px;
    font-weight: bold;
}
QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    padding: 4px 8px;
    background: white;
}
```

按钮样式：
- 主要操作（保存、刷新、测试）：蓝色背景 #2196F3，白色文字
- 危险操作（删除、重置）：红色背景 #f44336，白色文字
- 普通按钮：白色背景，灰色边框
- 统一高度36px，圆角6px，padding 8px 16px

输入框样式：
```css
QLineEdit, QSpinBox, QDoubleSpinBox, QComboBox {
    height: 32px;
    border: 1px solid #d0d0d0;
    border-radius: 4px;
    padding: 4px 8px;
    background: white;
}
QLineEdit:focus, QSpinBox:focus, QDoubleSpinBox:focus, QComboBox:focus {
    border: 2px solid #2196F3;
}
```

=== 功能保持 ===

确保所有现有功能完全保留：
- 配置的读取和保存逻辑不变
- 所有按钮的槽函数连接正确
- 与pet_core的交互接口不变
- 应用映射、网站规则、过滤器的编辑功能完整
- AI测试连接、刷新模型列表等功能正常

=== 迁移策略 ===

1. 先创建新的tab结构和左侧边栏
2. 逐页迁移内容，每迁移一页就测试读写配置
3. 保留原有的_save_all()、_load_all()逻辑
4. 删除旧的tab_basic、tab_behavior等，替换为新结构
5. 更新底部的"保存"和"取消"按钮的位置（固定在整个对话框底部）

请分步骤实现，每完成一个tab就输出代码，让我确认后再继续。